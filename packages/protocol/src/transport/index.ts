/**
 * Placeholder for the `Transport` interface + shared helpers.
 *
 * Phase 2 ships only the schemas + types layer; the universal transport
 * surface (Transport interface + bufferTransport / inMemoryPair / wsClient)
 * lands in Phase 3. This stub exists so the `./transport` subpath export
 * declared in package.json resolves and the package shape stays stable
 * across phases.
 */
export {};
