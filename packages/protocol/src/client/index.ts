/**
 * Placeholder for `ClaudeClient` (consumer-side principal).
 *
 * Phase 2 ships only the schemas + types layer; the universal `ClaudeClient`
 * class lands in Phase 3. This stub exists so the `./client` subpath export
 * declared in package.json resolves and the package shape stays stable across
 * phases.
 */
export {};
