/**
 * Placeholder for `ClaudeProcess` (binary-side principal).
 *
 * Phase 2 ships only the schemas + types layer; the universal `ClaudeProcess`
 * class lands in Phase 3. This stub exists so the `./process` subpath export
 * declared in package.json resolves and the package shape stays stable across
 * phases.
 */
export {};
