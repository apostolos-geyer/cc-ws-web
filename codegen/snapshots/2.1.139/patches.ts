/**
 * Hand-authored overrides for the 2.1.139 canonical. Each patch carries a
 * `reason` for the audit trail. Phase 2 fills these in; the seed is empty.
 */

import { definePatches } from "../../patch-dsl";

export default definePatches({ version: "2.1.139", patches: [] });
