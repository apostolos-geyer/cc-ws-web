/**
 * Hand-authored overrides for the 2.1.137 canonical. Committed alongside the
 * canonical for diff/audit only — the active version is 2.1.139.
 */

import { definePatches } from "../../patch-dsl";

export default definePatches({ version: "2.1.137", patches: [] });
